import { TestBed } from '@angular/core/testing';import { AlertService } from '../alert.service';import { tick } from '@angular/core/testing';describe('AlertService', () => {  let service: AlertService;  beforeEach(() => TestBed.configureTestingModule({}));  beforeEach(function() {    jasmine.clock().install();  });  afterEach(function() {    jasmine.clock().uninstall();  });  it('should be created', () => {    service = TestBed.get(AlertService);    expect(service).toBeTruthy();  });  it('should be clearAlert', () => {    service.clearAlert();  });  it('should be removeAlert', () => {    service.removeAlert({      message: '',      type: ''    });  });  it('should be setAlert', () => {    service.alertList = [];    service.setAlert({      message: '1',      type: '2'    });    expect(service.alertList).toEqual([{      message: '1',      type: '2'    }]);    service.setAlert({      message: '1',      type: '2'    });    expect(service.alertList).toEqual([{      message: '1',      type: '2'    }]);    service.setAlert({      message: '100',      type: 'error'    });    jasmine.clock().tick(50000);    service.setAlert({      message: '100',      type: 'error'    }, 100);    jasmine.clock().tick(100);  });});